{
    "buildCommand": "npm run build",
    "installCommand" : "npm install",
    "outputDirectory" : "./dist",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "./server.js"
        }
    ]
}