{
    "buildCommand": "npm run build",
    "installCommand" : "npm install",
    "outputDirectory" : "./dist",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "./dist/server.js"
        }
    ]
}