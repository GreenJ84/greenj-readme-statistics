{
    "buildCommand": "npm run build",
    "installCommand" : "npm install",
    "outputDirectory" : "dist",
    "rewrites": [
        {
            "src": "/(.*)",
            "destination": "./dist/src/server.js"
        }
    ]
}