query ($username: String!, $start: Int!, $end: Int!) {
        user(login: $username) {
            createdAt
            contributionsCollection(from: $start, to: $end) {
                contributionYears
                contributionCalendar {
                    weeks {
                        contributionDays {
                            contributionCount
                            date
                        }
                    }
                }
            }
        }
    }"